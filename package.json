{
  "name": "news-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal mobile dashboard: balanced news, GenAI tips, weather for Minneapolis & Puerto Escondido.",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "build:data:force": "FORCE_RUN=1 node scripts/build-data.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "node scripts/serve.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
